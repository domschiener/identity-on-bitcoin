var request = require('request');
var fs = require('fs');


/**request('http://localhost:5000/api/v1.0/auth', function (error, response, body) {
	if (!error && response.statusCode == 200) {
		console.log(body) // Show the HTML for the Google homepage.
	}
});**/